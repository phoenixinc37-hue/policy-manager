# 2026-04-29

- Policy Manager V10 blank shell work continued from FD1-derived copy at `sandbox/policy-manager-V10`.
- Verified live V10 URL in use: `https://policy-manager-v10.vercel.app`.
- Scott requested home page wording change from "Blank Template" to `Business Inc` for V10.
- Scott requested V10 home page changes: remove wording under Leadership Dashboard, install 3 panels for Policy / SOG / Memos, remove remaining "Blank Template" wording, and use the home page filing cabinet graphic as the source model for future page-by-page rollout.
- Durable implementation lesson: do not mass-replace shared UI graphics across many pages in one sweep when the code patterns differ; standardize the component first, then install page by page.
- A reusable cabinet component was created in `sandbox/policy-manager-V10/app/cabinet.ts` with `CabinetGraphic` as the source model for future use.
- Scott approved a safer rollout approach: keep note of the file cabinet code and install it on each page as needed rather than bulk-swapping globally.
- Scott asked for a built-in Setup page so each company can configure its own Policy Manager instance.
- Recommended Setup-page structure for Policy Manager: Company Profile, People and Groups, Document Rules, Workflow Rules, Template Rules, and Library Rules.
- Initial V10 Setup page scaffold was created at `sandbox/policy-manager-V10/app/setup/page.tsx`, but deployment was blocked because earlier malformed cabinet-swap edits left other V10 pages broken and needing cleanup before clean build/deploy.
- Current known issue at compaction: V10 has malformed page code from the earlier cabinet swap and needs a cleanup pass on broken pages before the new Setup page can go live.
- New conversational workflow preference from Scott: when Kate offers multiple concrete suggestions/options, label them with selection codes like `S1`, `S2`, `S3` so Scott can answer precisely and Kate can track which suggestions were addressed.
- Follow-up clarification: if Kate presents a recommendation, choice, or optional path that expects confirmation or selection, it should be explicitly framed as a suggestion or question and coded properly so Scott does not miss it while switching between versions.
