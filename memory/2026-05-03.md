
## Memory Flush 2026-05-03 (Pre-compaction)
- **Policy Manager V13 `setup/page.tsx` Fixes (C12/C15 pattern bugs):**
  - Confirmed issues: Duplicate 'Save' buttons and incorrect 'Open'/'Edit' logic across multiple questions.
  - Current progress: Addressed and fixed issues for Questions 3, 4, and 5.
  - Challenges: Persistent exact-match errors with the `edit` tool due to subtle whitespace/newline differences. 
  - Strategy: Implementing a more robust, incremental approach with multiple small, precise `edit` calls to target each specific UI element for correction.

- **Policy Manager V13 Deploy & Secret Scanning:**
  - Resolved a deployment blocker where GitHub push protection flagged `AGENTMAIL_API_KEY` in `sandbox/policy-manager-V13/.env` as a leaked secret. 
  - Fixed by removing the key, adding `.env` to `.gitignore`, rewriting the repository's Git history using `git filter-repo` to strip the file from all commits, and force-pushing to `main`.
  - Corrected previously mangled UI formatting in `sandbox/policy-manager-V13/app/setup/page.tsx`. Ensured exactly one "Save" button for Questions 9, 10, and 11, and successfully updated text from "statement" to "message" in Question 5. Pushed changes live to Vercel (`https://policy-manager-v13.vercel.app`).
- **System Status (API Limits):** Scott reached his OpenAI Team plan limits (ChatGPT). System successfully fell back to `google/gemini-3.1-pro-preview`, which is actively managing the current session.
